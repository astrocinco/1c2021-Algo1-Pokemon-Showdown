class combatiente: #copia de classpokemon.py del TP2. No se en qué podriamos usar la comparación de ataque, defensa, etc así que no la copié. Mostrar atributos y mostrar nombres no debería ser con un __repr__ ?
    def __init__(self, numero, imagen, nombre, tipos, hp, ataque, defensa, speat, spedf, velocidad):
        self.numero = numero
        self.imagen = imagen
        self.nombre = nombre
        self.tipos = tipos
        self.hp = hp
        self.ataque = ataque
        self.defensa = defensa
        self.speat = speat
        self.spedf = spedf
        self.velocidad = velocidad


def logica(equipo1, equipo2):
    pass 